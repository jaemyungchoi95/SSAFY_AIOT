import { createContext } from 'react';

export const AlertStateContext = createContext(null);
export const ReportStateContext = createContext(null);
export const WarehouseStateContext = createContext(null);
